node_modules/
.next/
.env
.env.local
.vercel/
coverage/

# El catálogo no va al repo: 16.973 filas de dato de negocio.
# El archivo vive en Supabase Storage y se referencia desde import_batch.
data/*.xlsx
data/*.xls

# Histórico de pedidos para el analisis de F4 (PLAN-F4.md): datos de clientes reales.
# Se procesa offline, nunca entra a la base salvo las reglas candidatas ya aprobadas.
data/pedidos_*.csv
data/pedidos_*.xlsx
data/link_producto.xlsx

# Ranking de venta: agregado del historico, sin dato de cliente, pero expone volumenes
# de venta por tipo de producto. Se genera con scripts/historico/ranking-venta.ts --sql
# y se aplica a mano; no va al repo mientras sea publico.
supabase/ranking-venta.sql

# artefactos de build
tsconfig.tsbuildinfo
next-env.d.ts
